# PostgreSQL Database Configuration (Backup)
# Use this file to switch back to PostgreSQL when needed

spring.datasource.url=jdbc:postgresql://ep-hidden-frost-adf4ah6r-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=neondb_owner
spring.datasource.password=npg_A4CLyhwTsBS6

# JPA Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Configuration for PostgreSQL
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# To use this configuration:
# 1. Rename application.properties to application-h2.properties
# 2. Rename this file to application.properties
# 3. Add PostgreSQL dependency to pom.xml:
#    <dependency>
#        <groupId>org.postgresql</groupId>
#        <artifactId>postgresql</artifactId>
#        <scope>runtime</scope>
#    </dependency>
